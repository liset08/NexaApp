<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="corp.cemsa.nexaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".PortadaMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginMainActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Menu.MenuMainActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".HomeMainActivity"
            android:label="Supervición"
            android:parentActivityName=".Menu.MenuMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BuscarPlaca.BuscarPlacaMainActivity"
            android:label="Buscar"
            android:parentActivityName=".Menu.MenuMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BuscarPlaca.DetalleLineChartActivity"
            android:parentActivityName=".BuscarPlaca.BuscarPlacaMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AlarmaModulo.AlarmaMainActivity"
            android:parentActivityName=".Menu.MenuMainActivity"
            android:label="Alerta"

            android:theme="@style/AppTheme" />
        <activity
            android:name=".AlarmaModulo.ListaAlarmaMainActivity"
            android:parentActivityName=".AlarmaModulo.AlarmaMainActivity"
            android:label="Alerta"

            android:theme="@style/AppTheme" />
        <activity
            android:name=".AlarmaModulo.ReporteMainActivity"
            android:parentActivityName=".AlarmaModulo.AlarmaMainActivity"
            android:label="Reporte"

            android:theme="@style/AppTheme">

        </activity>
        <activity android:name=".AlarmaModulo.SegundoChartMainActivity"
            android:label="Reporte"

            android:theme="@style/AppTheme">

        </activity>
    </application>

</manifest>